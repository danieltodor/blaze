{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make build project",
            "type": "shell",
            "command": "make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            // pip install compiledb
            "label": "compiledb create compile_commands.json",
            "type": "shell",
            "options": {
                "shell": {
                    "args": ["-i"]
                }
            },
            "command": "compiledb --no-build make"
        }
    ]
}
