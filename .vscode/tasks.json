{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make build active file",
            "type": "shell",
            "command": "make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
